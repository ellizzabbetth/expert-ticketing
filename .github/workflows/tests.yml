name: tests

on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: git rm --cached path/to/submodulerm -rf path/to/submodule
      - run: cd auth && npm install && npm run test:ci
